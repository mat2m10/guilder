<%= simple_form_for [craft, booking] do |f| %>
  <p>Enter a suitable timeframe to schedule a workshop. The craftman will follow up with you</p>
  <%= f.input :date %>
  <p>Add any other relevant information</p>
  <%= f.input :description %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>